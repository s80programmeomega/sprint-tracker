# pages/

Full-page components — one file per route.

Each file maps to a URL and is loaded by Vue Router into <RouterView />.

Examples:
- LoginPage.vue       → /login
- ProjectsPage.vue    → /projects
- SprintBoardPage.vue → /projects/:id/sprints/:sprintId

Naming convention: always suffix with "Page" to distinguish from reusable components.
