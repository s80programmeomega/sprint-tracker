# assets/

Static files — images, fonts, icons, global CSS.

These are processed by Vite at build time. Import them in components or CSS files using relative paths.

Examples:
- logo.svg         → app logo
- avatar-default.png → default user avatar
- main.css         → global CSS reset and base styles (if not using a UI library)
